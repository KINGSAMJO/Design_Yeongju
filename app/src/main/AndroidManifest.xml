<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.techtown.sopt_30th">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project_220402">
        <activity
            android:name=".semina2.SampleRecyclerView"
            android:exported="true" />
        <activity
            android:name=".semina2.FragTestActivity"
            android:exported="true" />
        <activity
            android:name=".semina1.HomeActivity"
            android:exported="true" />
        <activity
            android:name=".semina1.SignUpActivity"
            android:exported="true" />
        <activity
            android:name=".semina1.SignInActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>